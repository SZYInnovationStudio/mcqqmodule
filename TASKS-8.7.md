# 8.7.0 只增模块清单

- [x] 后台新增独立“增量模块”页面。
- [x] 登录管理员可上传 `.szydmodule` JSON 包。
- [x] 安装前检查格式、模块 ID、版本、文件数量、大小、路径和逐文件 SHA-256。
- [x] 模块只能写入自己的 `data/modules/<模块ID>`，不能写入核心目录。
- [x] 已存在的模块 ID 拒绝覆盖，新增功能必须使用新 ID。
- [x] 模块页面和接口使用独立命名空间。
- [x] 安装成功后立即加载，服务端重启后自动恢复。
- [x] 模块状态使用各自独立的 `state.json`。
- [x] 写明 `MODULE-SPEC.md` 技术规范。
- [x] 测试重复安装、错误哈希、路径穿越、核心文件写入和重启恢复。
